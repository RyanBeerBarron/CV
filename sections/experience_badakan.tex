\def \ExperienceTitle {\langstr{experience}{expérience}}

% Badakan points
\def \ExperienceBadakanDate {\langstr
	{Feb 2024 -- Present \textit{(2 years)}}
	{Fév 2024 -- Aujourd'hui \textit{(2 années)}}
}
\def \ExperienceBadakanRole {\langstr{Backend engineer}{Ingénieur backend}}
\def \ExperienceBadakanCompany {\langstr
	{Startup environment. \textbf{SaaS platform} for HR in hotels and restaurants. Clients include large Parisian palaces}
	{Environnement startup. Plateforme \textbf{SaaS SIRH} pour l'hôtellerie/restauration. Clients inclus des palaces Parisiens}
}
\def \ExperienceBadakanTechnicalContext {\langstr
	{Multitenant architecture, single monolith built with \textbf{Java 25} and \textbf{MongoDB}. Deployed on EKS (\textbf{AWS and Kubernetes})}
	{Architecture multitenant avec un seul monolithe en \textbf{Java 25} et \textbf{MongoDB}.}
}

\def \ExperienceBadakanTechnicalContextExtra {\langstr
	{Team worked with continuous deployments. App deployed in \textbf{EKS} and artifacts saved in \textbf{s3}}
	{Equipe fonctionne en déploiement continu. Application déployée dans \textbf{EKS} et artefactes dans \textbf{s3}}
}

\def \ExperienceBadakanFeatures {\langstr
	% TODO:
	{Built core features to onboard our biggest client. Includes:}
	{Developpé les fonctionnalités principales pour l'onboarding de notre plus gros client:}
}

\def \ExperienceBadakanFeaturePS {\langstr
    {
	\resumeItem{A timesheet adjustment system}
	\resumeItem{Integration with the customer's payroll system}
	\resumeItem{A planning assistance module}
	\resumeItem{Fixed price billing module for temporary workers}
    }
    {
	\resumeItem{Système de régularisation des feuilles de temps}
	\resumeItem{Intégration avec leur système de paie}
	\resumeItem{Module d'aide à la planification}
	\resumeItem{Rémunération à la vacation pour les travailleurs extra}
    }

}

\def \ExperienceBadakanMongodb {\langstr
	{Extensive MongoDB work including: optimising queries, workflows and indexing strategies.}
	{J'ai beaucoup travaillé sur la partie MongoDB. Optimisé les requêtes, les indexes et les workflows.}
}

\def \ExperienceBadakanMongodbCoveredQuery {\langstr
	{Resolved a bottleneck on our \textbf{candidate search} using a \textbf{covered query} (no disk lookup).}
	{Résolution d'un bottleneck sur la \textbf{recherche des candidats} à l'aide d'une \textbf{requête couverte} (pas d'utilisation du disque).}

}

\def \ExperienceBadakanMongodbWorkflow {\langstr
	{Optimized a client data replication workflow across environments, reducing execution time from ~2-3h to 1-3 min (\textbf{$\sim$97\% reduction})}
	{Optimisation d'un workflow de copie de données clients inter-environnements, réduisant le temps d'exécution de ~2-3h à 1-3 min (\textbf{réduction de $\sim$97\%})}
}

\def \ExperienceBadakanIncidents {\langstr
	{Resolved production incidents. Reduced \textbf{memory} footprint and \textbf{latency} of our most called endpoints. Reduced \textbf{p90} latency by \textbf{10x}}
	{Résolution d'incidents. Réduction de la \textbf{latence} et de l'empreinte \textbf{mémoire} des endpoints les plus utilisés. Réduction de la latence \textbf{p90} par 10}
}

\def \ExperienceBadakanProfiling {\langstr
	{Used \textbf{Asyncprofiler} for CPU and wall profiling; \textbf{VisualVM} and \textbf{heap dumps} for memory profiling.
	They allowed me to resolve I/O bottlenecks and OOM errors in production}
	{Utilisé \textbf{Asyncprofiler} pour le profilage CPU et wall ; \textbf{VisualVM} et des \textbf{heap dumps} pour le profilage mémoire.
	Cela m'a permis de résoudre des problèmes I/O et des erreurs OOM en production}
}

\def \ExperienceBadakanObservability {\langstr
	{Improved the observability stack. Added custom spans and metrics with attributes using \textbf{Opentelemetry} libraries.
	Gave us better insight into how our customers use the application. Allowed us to pinpoint performance problems ahead of time.}
	{Amélioration sur l'observabilité. Ajout de traces et métriques custom en utilisant \textbf{Opentelemetry}.
	 Nous a permis de mieux comprendre comment nos clients utilisent l'application. Nous a aidés à identifier en avance les problèmes de performance.}
}

\def \ExperienceBadakanConcurrency {\langstr
	{Resolved distributed concurrency issues: fixed incorrect application-level DB locks.
	Ensured backward/forward API compatibility for \textbf{zero down time} deployments.}
%
	{Résolu des problèmes de concurrence en environnement distribué: correction de lock DB incorrects.
	Maintien de la compatibilité 'backward' et 'forward' de l'API pour les déploiements \textbf{sans interruption de service.}}
}
